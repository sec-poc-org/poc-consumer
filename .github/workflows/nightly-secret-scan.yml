name: Nightly Secret Scan

on:
  schedule:
    - cron: '0 2 * * *'  # 02:00 UTC daily
  workflow_dispatch:

jobs:
  scan:
    uses: sec-poc-org/.github/.github/workflows/reusable-nightly-trufflehog.yml@main
    secrets: inherit
